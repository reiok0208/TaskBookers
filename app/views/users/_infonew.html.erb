<h1>User info</h1>

<h1>New book</h1>
<%= form_for(book) do |f| %>
<div class="field-style">
  <%= f.label :title %>
  <%= f.text_field :title %>
</div>
<div class="field-style">
  <%= f.label :body %>
  <%= f.text_area :body %>
</div>
<%= f.submit 'Create Book' %>
<% end %>